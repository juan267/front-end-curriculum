# Simon Dice

##Resumen

Para este reto, tu tarea es escribir el codigo necesario de jquery para replicar la funcionalidad del siguiente video:
[Simon dice](http://player.vimeo.com/video/65261103)

##Pasos

###Paso 0:

Modifica el codigo que te suplementamos para agregar la siguiente funcionalidad:

* Hacer click en el boton de `Click Me` crea un request de AJAX a la ruta `post /color`
* Cambia el `background-color` de una celda con el numero hexadecimal que el servidor devuelve. SIN MODIFICAR EL HTML

